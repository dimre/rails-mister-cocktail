<h1>Cocktails</h1>
<ul><% @cocktails.each do |cocktail| %>
  <li class="card-drink"><%= link_to cocktail.name, cocktail_path(cocktail) %> <%= cl_image_tag cocktail.photo.url(:small) %> </li>
  <% end %>
</ul>
<p class="btn btn-danger"><%= link_to 'Add a cocktail', new_cocktail_path %></p>
